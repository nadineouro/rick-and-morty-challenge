import { character } from "../../mocks";

export const mockSearchContent = {
  characters: [character],
  handleClickCard: () => {},
  modalOpen: true,
  handleClose: () => {},
  handlePageChange: () => {},
  currentPage: 1,
  totalPages: 10,
  activeCharacter: character,
};
